const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2); // Math é a API de operações matemáticas dentro do Js, O pow utilizado foi para criar a exponenciação

console.log(area.toFixed(2)); // toFixed para deixar a área com apenas duas casas decimais

console.log(typeof Math)